#include "pkgdef.hpp"

int createPkg(int content)
{
  int pkgCount;
  if (getPreciseNumber = true)
    pkgCount = pkgBytes / pkglength;
  if (getPreciseNumber = false)
    pkgCount = pkgs;
  if (pkgCount > max_pkgcount_per_connection)
    throw cntErr;
  if (pkgCount > max_pkgcount_per_device_alive_time)
    fpkg();
  if (getPreciseNumber == "")
    fatal("Code Error: src/software/createPkg.cpp line 14 column 28: Error with getPreciseNumber");